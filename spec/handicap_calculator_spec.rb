RSpec.describe HandicapCalculator do
  it "has a version number" do
    expect(HandicapCalculator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
